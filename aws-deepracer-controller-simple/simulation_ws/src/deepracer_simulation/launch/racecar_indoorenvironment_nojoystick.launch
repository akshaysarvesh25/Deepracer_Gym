<?xml version="1.0"?>
<launch>
	<!--<arg name="world_name" />-->
  <arg name="gui" default="true" />

  <include file="$(find gazebo_ros)/launch/empty_world.launch">
    <arg name="world_name" value="$(find deepracer_simulation)/worlds/willowgarage.world"/>
    <arg name="gui" value="$(arg gui)"/>
  </include>

  <param name="/use_sim_time" value="true"/>

  <!-- urdf xml robot description loaded on the Parameter Server, converting the xacro into a proper urdf file-->
  <param name="robot_description" command="$(find xacro)/xacro '$(find deepracer_simulation)/urdf/racecar.xacro'" />

  <!-- push robot_description to factory and spawn robot in gazebo -->
  <node name="racecar_spawn" pkg="gazebo_ros" type="spawn_model" output="screen" args="-urdf -param robot_description -model racecar -z 0.05 -x 5 -y 8.5" />

  <!-- ros_control racecar launch file -->
  <include file="$(find deepracer_simulation)/launch/racecar_control.launch" ns="/"/>
  <!--<include file="$(find deepracer_joy)/launch/deepracer_joy.launch" ns="/"/>-->
  <node name="foo_dropper" type="drop" pkg="topic_tools" args="/gazebo/model_states 99 100" />
  <node name="foo_dropper_1" type="drop" pkg="topic_tools" args="/scan 38 40" />

  <!-- Publish "better odom" topic that is normally generated by the particle filter -->
  <node name="better_odom" pkg="topic_tools" type="relay"
          args="/vesc/odom /pf/pose/odom" />

</launch>
